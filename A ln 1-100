A) Write a shell Script 
1) To find the largest Number from Two Numbers.(use the If --else construct)
2) To Display the sum of 1 to 100 numbers.(Use the for loop construct)

#!/bin/bash

# Script to find the largest number from two numbers

# Input two numbers
echo "Enter the first number:"
read num1
echo "Enter the second number:"
read num2

# Compare the two numbers using if-else construct
if [ $num1 -gt $num2 ]
then
    echo "$num1 is the largest number."
elif [ $num1 -lt $num2 ]
then
    echo "$num2 is the largest number."
else
    echo "Both numbers are equal."
fi


#!/bin/bash

# Script to display the sum of numbers from 1 to 100

# Initialize sum to 0
sum=0

# Use a for loop to add numbers from 1 to 100
for ((i=1; i<=100; i++))
do
    sum=$((sum + i))
done

# Display the sum
echo "The sum of numbers from 1 to 100 is: $sum"

OR 

# Program to find the largest number from two numbers

# Input two numbers
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

# Compare the two numbers using if-else construct
if num1 > num2:
    print(f"{num1} is the largest number.")
elif num1 < num2:
    print(f"{num2} is the largest number.")
else:
    print("Both numbers are equal.")


# Program to display the sum of numbers from 1 to 100

# Initialize sum to 0
sum = 0

# Use a for loop to add numbers from 1 to 100
for i in range(1, 101):
    sum += i

# Display the sum
print(f"The sum of numbers from 1 to 100 is: {sum}")
